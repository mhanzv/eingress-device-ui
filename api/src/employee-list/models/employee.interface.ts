export interface Employee {
  id?: number;
  fullname?: string;  
  phone?: string;
  email?: string;  
  role?: string;
  regdate?: Date;
  lastlogdate?: string;
  profileImage?: string;
  fingerprint?: string; // Include fingerprint property
  rfidtag?: string;
}

  

  